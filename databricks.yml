# The name of the bundle. run `databricks bundle schema` to see the full bundle settings schema.
bundle:
  name: mlopsllm

variables:
  experiment_name:
    description: Experiment name for the model training.
    default: /Users/${workspace.current_user.userName}/${bundle.target}-mlopsllm-experiment
  model_name:
    description: Model name for the model training.
    default: mlopsllm-model

include:
  # Resources folder contains ML artifact resources for the ML project that defines model and experiment
  # And workflows resources for the ML project including model training -> validation -> deployment,
  # batch inference, data monitoring, metric refresh, alerts and triggering retraining
  - ./resources/*.yml

# Deployment Target specific values for workspace
targets:
  dev:  # UC Catalog Name 
    default: true
    workspace:
      # TODO: add dev workspace URL
      host: https://adb-4496471258703562.2.azuredatabricks.net



  staging:
    workspace:
      host: https://adb-5565834108644774.14.azuredatabricks.net

  prod:
    workspace:
      host: https://adb-6698555481828834.14.azuredatabricks.net

  test:
    workspace:
      host: https://adb-5565834108644774.14.azuredatabricks.net

